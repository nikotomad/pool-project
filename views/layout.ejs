<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= title %></title>
    <link href="https://fonts.googleapis.com/css?family=Montserrat" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script type="text/javascript" src="/js/initMap.js"></script>
  </head>
  <body>
    <nav>
      <div class="usercp">
        <p>Bienvenido, <%= user.username %></p>
        <a href='/users/<%= user._id %>/edit'><button id="editProfile">Edita tu perfil</button></a>
        <a href='/logout'><button id="logout">Cerrar sesión</button></a>
        <% if(user.avatar !== undefined && user.avatar !== '') { %>
        <img src="/uploads/<%= user.avatar %>" height ="100px" width="100px" style="border-radius: 50%">
        <% } else { %>
        <img src="/uploads/missing.jpg" height ="100px" width="100px" style="border-radius: 50%">
        <% } %>
      </div>
    </nav>
    <div class="background">
      <%- body %>
    </div>
    <footer>
      <p>Nikolaas Verlee - Ernesto Monsalve</p>
      <p>&copy; IronProject2017</p>
    </footer>
  </body>
</html>
